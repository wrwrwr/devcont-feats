{
    "name": "Install using APT",
    "id": "apt-install",
    "version": "0.0.4",
    "description": "Installs packages assuming apt is available.",
    "options": {
        "name": {
            "type": "string",
            "proposals": [
                "git",
                "git,sudo"
            ],
            "default": "",
            "description": "Comma-separated list of package names."
        }
    }
}
